# DiceLab core features require no API keys or production secrets.
#
# Vite exposes variables prefixed with VITE_ to client-side code. Never put
# credentials, tokens, private keys, or other secrets in VITE_* variables.
#
# Optional development-only example:
# VITE_DICELAB_BUILD_LABEL=local
