{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal permissions required by the DiceLab main window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
